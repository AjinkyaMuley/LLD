#include "Splitwise.hpp"
#include <bits/stdc++.h>
using namespace std;

int main()
{
    Splitwise *splitwise = new Splitwise();
    splitwise->demo(); // Ensure demo is called to initialize and run the program

    return 0;
}